module imageDrawer(input logic clk, 
			input logic [9:0] hcount, vcount,
			output logic [7:0] pixel);
	
	logic [15:0] address;
	assign address=(765*hcount)+ vcount; 
	
	SDRAMController (address,clk,8'b0,1'b0,pixel);
	
endmodule